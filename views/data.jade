body
    h1= collection
    #objects
    h2= objects.length
        table(border=1)
          if objects.length > 0
              - each val, key in objects[objects.length - 1]
                  th= key 
          - each obj in objects
            tr.obj
              - each val, key in obj
                td.key= val